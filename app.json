{
    "addons": ["heroku-postgresql"],
    "buildpacks": [
        {
            "url": "heroku/php"
        },
        {
            "url": "heroku/nodejs"
        }
    ],
    "scripts": {
        "compile": [
            "@php artisan route:cache",
            "@php artisan event:cache",
            "@php artisan view:cache"
        ]
    }
}
